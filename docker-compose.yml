version: "3.9"

services:
  algo_trading:
    build: .
    container_name: algo_trading_app
    working_dir: /app
    env_file:
      - .env

    volumes:
      # Mount project folder so file changes reflect instantly (no rebuild needed)
      - .:/app
      - ~/.skills:/root/.skills

    ports:
      # Expose Streamlit port
      - "8501:8501"

    # Keep container running so you can exec into it
    stdin_open: true
    tty: true
